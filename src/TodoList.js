import React from "react";
import Typography from "@material-ui/core/Typography";

const TodoList = () => {
  return (
    <div>
      <Typography variant="h4">one todo</Typography>
      <Typography variant="h4">one todo</Typography>
      <Typography variant="h4">one todo</Typography>
    </div>
  );
};

export default TodoList;
